import java.io.BufferedReader;
import java.io.FileReader;

public class NotesOne {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		
		//problem 1
		//pull data from text file use scanner
		//encrypt text file including whitespace
		//each letter gets mapped to index 0=a 1=2 etc. store in 'alphabets'
		//loop, goes through each letter, and check if it's in the collection 'alphabets'
		//else put the actual value in the string
		//what the fuck is this encryption equation? use hashmap to store alphabet
		//display encrypted text
		
		System.out.println("Stuff");
		//BufferedReader lineRead = new BufferedReader(new InputStreamReader(system.in));
		
		//problem 2
		//buffered reader
		//BufferedReader br = new BufferedReader(new FileReader("input.txt")); //don't worry about exception handling for now
		//be sure to convert to all lower or upper case
		//don't use scanner to open dictionary file
		//form words by rearranging letters accepting word and new word length as arguments
		//checks dictionary file to see if the result is a word
		//must be read from command line. accept int number then string word
		//create a map of each character in teh input word and count the characters
		//open the dictionary and create a list of words that are equivalent to the length n read from command line
		//go through each word from teh list of extracted words from teh dictionary
		//create a map of each character in the dictionary word that maps to teh char count
		//got through each character in the extracted dictionary words and see if the character exists in teh input word and if hte character count matches then print that dictionary word
		
		//problem 3
		//

		/*
		 * Hierarchy of exceptions
		 * Throwable has 2 children: exception and error
		 * 
		 */
	}

}
